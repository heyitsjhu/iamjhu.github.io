<h1>Blog Index</h1>


<%= link_to "New Post", new_post_path %>
<ul id="posts">
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post_path(post), id: "post_#{post.id}" %></li>
  <% end %>
</ul>